apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-issuer
spec:
  vault:
    server: http://192.0.2.2:8200
    path: pki/sign/demo
    auth:
      tokenSecretRef:
        name: vault-token
        key: token
